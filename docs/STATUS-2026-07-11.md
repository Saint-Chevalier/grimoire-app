# Grimoire App — Status 2026-07-11

**Status:** GOOD TO GO (local loop) · OPEN-SOURCE PACK STARTED

## Product loop
1. Select Focus (sealed universe)
2. Speak / paste intel → universe densens
3. ★ HUD → Intel Atlas (read-only knowledge modal)
4. Cast Spell → consolidate → one READY spell
5. Copy → outside channel → paste reply → compound

## Stack
- `js/universe.js` — canvas cosmos
- `js/app.js` — loop, atlas, cast consolidate, pulse, images
- `js/data.js` — spells, alignment parse, dedupe
- `js/intelligence.js` — optional local vault FS writes
- `js/stars.js` — legacy metrics helpers

## Verify
Hard-refresh static/Vite serve → open Focus → ★ Atlas → × closes → Cast Spell restructures stack.

## Open-source notes
- Seeds markdown under `conversations/` and `spells/` are **examples**, not private vaults.
- Never commit `GRIMOIRE-FocusIntelligence/` folders generated on machine.
